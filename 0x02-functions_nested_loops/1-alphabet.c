#include "main.h"
/**
 * main - check the code.
 *
 * Description: it is what it is
 * Return: Always 0.
 */

void print_alphabet(void)
{
	int l = 'a';

	while (l <= 'z')
	{
		_putchar(l);
		l++;
	}
	_putchar(10);
}
